/config/config.php
/public/uploads/
*.log

# runner
/runner/state/
/runner/config.json

# local front-end verification harness (puppeteer-core + Edge)
/.verify/

# API keys - never commit
runner/deepseek.key
*.key

# environment-specific, gitignored per DabbleLabs convention
/deploy.config.json

# node tooling (one-off favicon generation via sharp) - not part of the PHP site
/node_modules/
/package.json
/package-lock.json
